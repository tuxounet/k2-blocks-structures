{
  "name": "@<%= partition %>/<%= name %>-provisioning",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prenuke": "npm run clean",
    "nuke": "rm -rf ./node_modules ./package-lock.json",
     
    "clean": "rm -rf ./dist ./out ./coverage",
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest --coverage",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all",
    "destroy": "cdk destroy --all"
  },
  "devDependencies": {
    "@types/jest": "^29.2.6",
    "@types/node": "18.11.18",
    "aws-cdk": "2.61.1",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "typescript": "~4.9.4"
  },
  "dependencies": {
    "@k2/builder": "^1.0.0",
    <% for(const component of components) { %>
    "@<%= partition %>/<%= name %>-<%= component.segment %>-infra" : "^1.0.0",
    <% } %>
    "aws-cdk-lib": "2.61.1",
    "constructs": "^10.0.0"
  }
}