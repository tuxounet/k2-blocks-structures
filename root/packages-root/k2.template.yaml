k2:
  metadata:
    id: k2.cli.create.packages-root
    kind: "template"
  body:
    name: "create"
    parameters:
      description: "all groups"
      groups: []

    scripts:
      post:
        - "npm install"
